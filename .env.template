# ============================================
# FIRE Platform Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# Updated: v1.0-auth-complete (Golden State)

# --- LOGTO AUTHENTICATION (Required) ---
# The Web App Keys (Traditional Web - for NextAuth.js)
LOGTO_APP_ID=your_logto_app_id
LOGTO_APP_SECRET=your_logto_app_secret

# The M2M Keys (Machine-to-Machine - for admin API operations)
LOGTO_M2M_APP_ID=your_m2m_app_id
LOGTO_M2M_APP_SECRET=your_m2m_app_secret

# Logto Endpoints (IMPORTANT: LOGTO_ISSUER must include /oidc path!)
LOGTO_ENDPOINT=http://localhost:3001
LOGTO_ISSUER=http://localhost:3001/oidc
LOGTO_API_RESOURCE=https://api.fire-platform.local

# --- NEXTAUTH CONFIGURATION (Required) ---
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32
AUTH_SECRET=same_as_nextauth_secret

# --- DATABASE (Required) ---
DATABASE_URL="postgresql://fireuser:firepass@localhost:5432/fire_db"

# --- MINIO/S3 STORAGE (Optional - for file uploads) ---
S3_ENDPOINT=http://localhost:9100
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin123
S3_BUCKET=fire-uploads
S3_REGION=us-east-1
S3_PUBLIC_URL=http://localhost:9100/fire-uploads

# --- EMAIL (Optional - for notifications) ---
# MAILGUN_API_KEY=your_mailgun_api_key
# MAILGUN_DOMAIN=your_domain.com
# MAILGUN_FROM_EMAIL=hello@your_domain.com

# ============================================
# PRODUCTION NOTES:
# ============================================
# 1. LOGTO_ISSUER MUST end with /oidc (e.g., https://auth.example.com/oidc)
# 2. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 3. Use different APP_ID/SECRET for dev vs production Logto instances
# 4. Never commit actual secrets to version control
